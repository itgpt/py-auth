# TypeScript 客户端构建

本文面向维护 `client/ts` 包的开发者。

## 构建

```bash
cd client/ts
pnpm build
```

当前使用 `tsconfig.build.json`，仅编译 `src/`。

## 常用命令

```bash
pnpm typecheck
pnpm build:example
pnpm example:run
```
